sudo: required

language: generic

services:
  - docker

before_install:
  - docker build -t hellomoto177/docker-react -f Dockerfile.dev .

script:
  - docker run hellomoto177/docker-react npm run test -- --coverage